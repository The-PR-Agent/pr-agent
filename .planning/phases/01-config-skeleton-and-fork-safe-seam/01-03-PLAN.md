---
phase: 01-config-skeleton-and-fork-safe-seam
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - tests/unittest/test_describe_byte_identical_when_off.py
autonomous: true
requirements: [CFG-05]
must_haves:
  truths:
    - "With both toggles off (the shipped default), _prepare_pr_answer produces exactly the same (title, body) output as before the fork seams were introduced — a golden-output characterization proves 0-byte drift."
    - "Every fork-added line in pr_description.py that could alter describe behavior is gated behind get_settings().pr_description.get(<flag>, False), so upstream rebases only conflict on toggle-reading lines."
  artifacts:
    - "tests/unittest/test_describe_byte_identical_when_off.py"
  key_links:
    - "Golden characterization pins ALL inputs (fixed self.data, fixed vars, mocked git provider) so the assertion is deterministic"
    - "Guard-audit reads pr_description.py source and asserts the loader helper and any fork seam are unreachable when toggles are off"
---

<objective>
Prove the gating success criterion: with both toggles off, `describe` output is byte-identical to upstream. Add a golden-output characterization test on `_prepare_pr_answer` (all inputs pinned) asserting the exact `(title, body)` result with defaults off, plus a guard-audit test asserting the fork-added seams in `pr_description.py` are all `.get(<flag>, False)`-guarded (or inert/unwired) so they cannot change behavior when off.

Purpose: Lock in CFG-05 (byte-identical-when-off) and the fork-safe seam convention (criterion #4) before any feature code lands in Phases 2/3.
Output: A characterization + guard-audit test file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-config-skeleton-and-fork-safe-seam/01-RESEARCH.md
@pr_agent/tools/pr_description.py
@tests/unittest/test_pr_description_output_core.py
@tests/unittest/test_pr_description.py
</context>

## Phase Goal

**As a** fork maintainer, **I want to** prove `describe` output is unchanged when both toggles are off and that every fork seam is toggle-guarded, **so that** downstream users see zero behavior change by default and upstream rebases stay conflict-free outside the toggle lines.

<tasks>

<task type="auto">
  <name>Task 1: Golden-output characterization of _prepare_pr_answer with toggles off</name>
  <files>tests/unittest/test_describe_byte_identical_when_off.py</files>
  <read_first>
    - pr_agent/tools/pr_description.py (`_prepare_pr_answer` lines 551-625 — the exact assembly logic being pinned)
    - tests/unittest/test_pr_description_output_core.py (lines 1-90 — the `_make_instance` bypass-__init__ pattern, `_settings` mock builder, and existing `_prepare_pr_answer` output assertions to mirror)
    - .planning/phases/01-config-skeleton-and-fork-safe-seam/01-RESEARCH.md (section 5 — byte-identical strategy; section 7 — pin ALL inputs landmine)
  </read_first>
  <action>
    Create `tests/unittest/test_describe_byte_identical_when_off.py`. Reuse the `_make_instance` (bypass `__init__` via `patch.object(PRDescription, "__init__", ...)`) and settings-mock patterns from `test_pr_description_output_core.py`. Build a PRDescription instance with a FIXED `self.data` (a small deterministic dict, e.g. `{"type": ["Enhancement"], "description": "Adds a feature"}`), fixed `self.vars` (including a fixed `title`), and a mocked git provider whose `is_supported` returns deterministic values. Mock `get_settings()` so `pr_description.enable_conventional_title` and `pr_description.enable_org_template` both read `False` via `.get(<flag>, False)` (the shipped defaults), alongside the other pr_description keys `_prepare_pr_answer` reads (`enable_pr_type`, `generate_ai_title`, `enable_semantic_files_types`, etc.), pinned to fixed values. Call `_prepare_pr_answer()` and assert the returned `(title, pr_body)` equals an exact golden string literal captured for this fixed input. The golden string is the current upstream output for that input — capture it by running the function once during implementation and freezing the literal into the test (this is the "0-byte diff vs unpatched upstream" proof available without a second checkout, per RESEARCH section 5). Pin EVERY input so the assertion is deterministic (RESEARCH section 7). This test will FAIL loudly if any Phase-2/3 change ever leaks behavior into the toggles-off path.
  </action>
  <acceptance_criteria>
    - `pytest tests/unittest/test_describe_byte_identical_when_off.py` exits 0.
    - The test calls `_prepare_pr_answer()` with both fork toggles mocked to `False`.
    - The test asserts the returned `title` and `pr_body` against exact golden string literals (not substring/contains checks).
    - All inputs (`self.data`, `self.vars`, git-provider `is_supported`, settings) are pinned to fixed values inside the test.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/unittest/test_describe_byte_identical_when_off.py -x -q</automated>
  </verify>
  <done>The golden characterization passes, proving `_prepare_pr_answer` output is byte-identical to upstream when both toggles are off.</done>
</task>

<task type="auto">
  <name>Task 2: Guard-audit that fork seams are toggle-gated or inert</name>
  <files>tests/unittest/test_describe_byte_identical_when_off.py</files>
  <read_first>
    - pr_agent/tools/pr_description.py (the loader helper + constant added in Plan 01 near the imports; and any seam near lines 120-194 / 551-625)
    - .planning/phases/01-config-skeleton-and-fork-safe-seam/01-RESEARCH.md (section 4 — guarding rule; criterion #4)
  </read_first>
  <action>
    Add a guard-audit test to the same file that reads the `pr_description.py` source text (resolve its path from the imported module's `__file__`) and asserts the fork-safe-seam invariant for Phase 1: (a) the fork loader `load_org_template` is defined but NOT invoked anywhere in the module body outside its own definition (assert the source contains exactly one occurrence of `load_org_template(` — the def line's usage count is zero calls this phase, so the token `load_org_template(` appears once as the `def`); (b) if either flag name `enable_conventional_title` or `enable_org_template` appears anywhere in `pr_description.py`, it appears only inside a `.get("<flag>", False)` accessor form (assert no bare attribute-access occurrences like `pr_description.enable_conventional_title` without `.get(`). Since Phase 1 wires no feature behavior, the strongest assertion is that neither flag is referenced in `pr_description.py` yet at all, OR — if a reference exists — it is exclusively the `.get(<flag>, False)` form. Encode the check to accept either posture (zero references, or references only via `.get`), and to FAIL if a bare attribute access to either flag exists. This enforces criterion #4 (upstream rebases conflict only on `.get`-guarded toggle-reading lines) and the RESEARCH section-7 attribute-vs-`.get` crash landmine. Do NOT use a bare `grep -c ... == 0` on the raw file (comment-text discipline); the check operates on parsed occurrences of the accessor forms in Python source, not on comment prose.
  </action>
  <acceptance_criteria>
    - `pytest tests/unittest/test_describe_byte_identical_when_off.py` exits 0 (both tests).
    - The audit asserts `load_org_template` is not called anywhere in `pr_description.py` this phase (defined only).
    - The audit asserts that any occurrence of `enable_conventional_title` / `enable_org_template` in `pr_description.py` uses the `.get("<flag>", False)` form, and fails on a bare attribute access.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/unittest/test_describe_byte_identical_when_off.py -x -q</automated>
  </verify>
  <done>The guard-audit passes, proving fork seams are inert/toggle-gated and the loader is unwired this phase.</done>
</task>

<task type="auto">
  <name>Task 3: Full describe test suite regression check</name>
  <files>tests/unittest/test_describe_byte_identical_when_off.py</files>
  <read_first>
    - tests/unittest/test_pr_description.py (existing describe tests that must still pass)
    - tests/unittest/test_pr_description_output_core.py (existing output-core tests that must still pass)
  </read_first>
  <action>
    No new code — this task is a regression gate. Run the existing describe test suites (`test_pr_description.py` and `test_pr_description_output_core.py`) together with the two new tests to confirm the Plan 01 seam additions (the loader helper + module-level constant + import of `Path`) introduced no regression in the existing `_prepare_pr_answer` / `_prepare_pr_answer_with_markers` / `process_description` behavior. If any existing test fails, the fork seam was NOT inert — fix the seam (not the existing test) so upstream behavior is preserved. This closes the loop on CFG-05 at the suite level, complementing the golden characterization at the unit level.
  </action>
  <acceptance_criteria>
    - `pytest tests/unittest/test_pr_description.py tests/unittest/test_pr_description_output_core.py tests/unittest/test_describe_byte_identical_when_off.py` exits 0.
    - No existing describe test is modified to make it pass (only seam code may be corrected if a regression surfaces).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/unittest/test_pr_description.py tests/unittest/test_pr_description_output_core.py tests/unittest/test_describe_byte_identical_when_off.py -q</automated>
  </verify>
  <done>All existing describe tests plus the new characterization/guard-audit tests pass, confirming the Phase-1 seam is inert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test -> source | The guard-audit reads `pr_description.py` source text to assert the seam invariant; no untrusted input, no execution of external data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Tampering | toggles-off code path | medium | mitigate | Golden characterization pins the exact `_prepare_pr_answer` output with toggles off; any future behavior leak into the default path fails the test. |
| T-01-08 | Repudiation | fork-seam guarding | low | mitigate | Guard-audit asserts every flag reference uses `.get(<flag>, False)` and the loader is unwired, so the defaults-off contract is machine-checked, not assumed. |
| T-01-09 | Elevation of Privilege | inert loader | low | accept | `load_org_template` is defined but uncalled this phase; no reachable behavior change. |
</threat_model>

<verification>
- `python -m pytest tests/unittest/test_describe_byte_identical_when_off.py -x -q` exits 0.
- Golden characterization asserts exact `(title, pr_body)` for a fully-pinned input with both toggles off.
- Guard-audit asserts fork flags (if referenced) use `.get(<flag>, False)` only, and the loader is uncalled.
- Full describe suite regression passes: `pytest tests/unittest/test_pr_description.py tests/unittest/test_pr_description_output_core.py tests/unittest/test_describe_byte_identical_when_off.py` exits 0.
</verification>

<success_criteria>
- CFG-05 satisfied: byte-identical-when-off proven by a deterministic golden characterization plus a full-suite regression, and the fork seam convention (criterion #4) is machine-enforced by the guard-audit.
</success_criteria>

<artifacts_this_plan_produces>
- New file: `tests/unittest/test_describe_byte_identical_when_off.py` (golden characterization + guard-audit + regression gate)
- No new source symbols or config keys (verification-only plan)
</artifacts_this_plan_produces>

<output>
Create `.planning/phases/01-config-skeleton-and-fork-safe-seam/01-03-SUMMARY.md` when done.
</output>
