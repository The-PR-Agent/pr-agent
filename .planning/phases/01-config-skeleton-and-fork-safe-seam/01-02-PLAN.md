---
phase: 01-config-skeleton-and-fork-safe-seam
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - tests/unittest/test_org_toggles_env_override.py
  - docs/fork/org-mr-enhancements.md
autonomous: true
requirements: [CFG-06]
must_haves:
  truths:
    - "An operator can enable either toggle for a single CI invocation by setting PR_DESCRIPTION__ENABLE_CONVENTIONAL_TITLE=true or PR_DESCRIPTION__ENABLE_ORG_TEMPLATE=true, without editing configuration.toml."
    - "The env override is proven by a test that builds a fresh Dynaconf (not the already-constructed singleton) so the env value is actually bound."
    - "Both env vars are documented in fork-owned docs."
  artifacts:
    - "tests/unittest/test_org_toggles_env_override.py"
    - "docs/fork/org-mr-enhancements.md"
  key_links:
    - "Env override rides the existing config_loader.py:12-18 dynaconf env_loader (envvar_prefix=False, merge_enabled=True) — NO new plumbing added"
    - "Test builds a fresh Dynaconf with the same kwargs pointing at configuration.toml, because dynaconf binds env at construction time"
---

<objective>
Prove and document that the two toggles are environment-overridable through PR-Agent's existing dynaconf `env_loader`, with no new plumbing. Add a test that sets `PR_DESCRIPTION__ENABLE_CONVENTIONAL_TITLE=true` / `PR_DESCRIPTION__ENABLE_ORG_TEMPLATE=true`, constructs a fresh `Dynaconf` (the singleton is already built and cannot pick up late env changes), and asserts the flag reads `True`. Add fork-owned docs describing both env vars.

Purpose: Satisfy CFG-06 (verify-and-document only) — enable per-CI-invocation toggling without editing a config file.
Output: An env-override test and a fork docs page.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-config-skeleton-and-fork-safe-seam/01-RESEARCH.md
@pr_agent/config_loader.py
@tests/unittest/test_extra_config_url.py
@tests/unittest/test_config_loader_secrets.py
</context>

## Phase Goal

**As a** CI operator, **I want to** flip either org-MR toggle via an environment variable for a single pipeline run, **so that** I can enable the feature per-invocation without committing a config change.

<tasks>

<task type="auto">
  <name>Task 1: Prove env-var override with a fresh-Dynaconf test</name>
  <files>tests/unittest/test_org_toggles_env_override.py</files>
  <read_first>
    - pr_agent/config_loader.py (lines 10-40 — the exact `dynconf_kwargs` and `Dynaconf(...)` construction to mirror)
    - tests/unittest/test_config_loader_secrets.py (config-construction test pattern)
    - tests/unittest/test_extra_config_url.py (lines 1-30 — env/config test conventions, os import usage)
    - .planning/phases/01-config-skeleton-and-fork-safe-seam/01-RESEARCH.md (sections 2 and 7 — env test isolation landmine)
  </read_first>
  <action>
    Create `tests/unittest/test_org_toggles_env_override.py`. The test MUST NOT rely on the already-imported `get_settings()` singleton (dynaconf binds env at construction, so the singleton will not see a late `os.environ` change — this is the isolation landmine from RESEARCH section 7). Instead: set the env var (use `monkeypatch.setenv` so it auto-restores, or set/pop `os.environ` in a try/finally), then construct a FRESH `Dynaconf` mirroring `config_loader.py`'s `dynconf_kwargs` (`core_loaders=[]`, `loaders=['pr_agent.custom_merge_loader', 'dynaconf.loaders.env_loader']`, `merge_enabled=True`, `envvar_prefix=False`, `load_dotenv=False`) with `settings_files` pointing at the real `pr_agent/settings/configuration.toml` (resolve its absolute path from the installed `pr_agent` package, e.g. via `pr_agent.config_loader.current_dir`). Write two tests: (a) set `PR_DESCRIPTION__ENABLE_CONVENTIONAL_TITLE=true`, build the fresh Dynaconf, assert `.pr_description.enable_conventional_title is True`; (b) set `PR_DESCRIPTION__ENABLE_ORG_TEMPLATE=true`, build fresh, assert `.pr_description.enable_org_template is True`. Optionally add a control assertion that WITHOUT the env var a fresh Dynaconf reads the flag as `False` (defaults preserved). This proves the double-underscore `SECTION__KEY` mapping works with `envvar_prefix=False` and adds no new plumbing (CFG-06).
  </action>
  <acceptance_criteria>
    - `pytest tests/unittest/test_org_toggles_env_override.py` exits 0.
    - Test (a) asserts `.pr_description.enable_conventional_title is True` when `PR_DESCRIPTION__ENABLE_CONVENTIONAL_TITLE=true` is set.
    - Test (b) asserts `.pr_description.enable_org_template is True` when `PR_DESCRIPTION__ENABLE_ORG_TEMPLATE=true` is set.
    - The test constructs a fresh `Dynaconf` rather than reusing the module-level singleton.
    - The env var is removed/restored after each test (no leakage to other tests).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/unittest/test_org_toggles_env_override.py -x -q</automated>
  </verify>
  <done>Both env vars provably flip their toggle to `True` through the existing dynaconf env_loader, with test isolation preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Document both env vars in fork-owned docs</name>
  <files>docs/fork/org-mr-enhancements.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 1 success criterion #5 — the documentation requirement)
    - pr_agent/settings/configuration.toml (the fork toggle block added in Plan 01, whose comment points at this doc path)
  </read_first>
  <action>
    Create `docs/fork/org-mr-enhancements.md` (fork-owned; the `configuration.toml` fork comment from Plan 01 references this exact path). Document: (1) the two `[pr_description]` toggles `enable_conventional_title` and `enable_org_template`, both defaulting to `false`; (2) that each can be enabled per-CI-invocation via the environment using PR-Agent's existing dynaconf env override with the `SECTION__KEY` double-underscore convention and no prefix — give the two concrete env var names `PR_DESCRIPTION__ENABLE_CONVENTIONAL_TITLE=true` and `PR_DESCRIPTION__ENABLE_ORG_TEMPLATE=true`; (3) note that env vars must be set in the real process environment (not a `.env` file, since `load_dotenv=False`). Keep it a concise reference page. Do NOT document Phase 2/3 feature behavior (title rewriting, template prepend) beyond a one-line "what the toggle will gate" note — those ship later.
  </action>
  <acceptance_criteria>
    - `docs/fork/org-mr-enhancements.md` exists.
    - It contains the literal env var names `PR_DESCRIPTION__ENABLE_CONVENTIONAL_TITLE` and `PR_DESCRIPTION__ENABLE_ORG_TEMPLATE`.
    - It states both toggles default to `false`.
    - It notes env vars must be set in the process environment (not `.env`).
  </acceptance_criteria>
  <verify>
    <automated>python -c "import pathlib; t=pathlib.Path('docs/fork/org-mr-enhancements.md').read_text(encoding='utf-8'); assert 'PR_DESCRIPTION__ENABLE_CONVENTIONAL_TITLE' in t; assert 'PR_DESCRIPTION__ENABLE_ORG_TEMPLATE' in t; assert 'false' in t.lower(); print('ok')"</automated>
  </verify>
  <done>Both env vars and the defaults-off behavior are documented in a fork-owned page referenced by the configuration.toml fork comment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| environment -> process | Env vars `PR_DESCRIPTION__*` are read by dynaconf at construction; values are coerced to booleans |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Elevation of Privilege | env-var toggle | low | accept | Env override only flips two boolean feature gates; both ship `false`, and the feature bodies are inert until Phases 2/3. Setting an env var requires control of the CI environment already. |
| T-01-05 | Tampering | test env isolation | low | mitigate | Test sets/restores env vars (monkeypatch or try/finally) and builds a fresh Dynaconf, so no env leakage corrupts other tests or the singleton. |
| T-01-06 | Information Disclosure | fork docs | low | accept | Docs describe public config keys only; no secrets. |
</threat_model>

<verification>
- `python -m pytest tests/unittest/test_org_toggles_env_override.py -x -q` exits 0.
- Both env vars flip their respective toggle to `True` via a freshly constructed Dynaconf.
- `docs/fork/org-mr-enhancements.md` documents both env vars and the defaults-off behavior.
- No new plumbing added to `config_loader.py` (verify-and-document only).
</verification>

<success_criteria>
- CFG-06 satisfied: both toggles proven env-overridable through the existing dynaconf `env_loader`, and documented in fork-owned docs.
</success_criteria>

<artifacts_this_plan_produces>
- New file: `tests/unittest/test_org_toggles_env_override.py`
- New file: `docs/fork/org-mr-enhancements.md`
- No new config keys or source symbols (relies on Plan 01's toggles and the existing env_loader)
</artifacts_this_plan_produces>

<output>
Create `.planning/phases/01-config-skeleton-and-fork-safe-seam/01-02-SUMMARY.md` when done.
</output>
